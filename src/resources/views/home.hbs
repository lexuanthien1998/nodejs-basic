<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trang tin tức công nghệ hàng đầu Việt Nam</title>
</head>
<body>
    <div>
        <form method="POST">
            <div class="form-group">
                <label for="exampleInputEmail1">Tìm kiếm</label>
                <input type="text" class="form-control" id="keyword" name="keyword" aria-describedby="emailHelp" placeholder="Nhập từ khóa tìm kiếm...">
            </div>
            
            <button type="submit" class="btn btn-primary">Search</button>
        </form>
    </div>
    <hr>
    <div class="row mt-3">
        {{#each course}}
            <div class="col-6">
                <p>{{ this.title }}</p>
                <p>{{ this.description}}</p>
            </div>
        {{/each}} 
        {{!--loop each of https://handlebarsjs.com/ --}}
    </div>
</body>
</html>